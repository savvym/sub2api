// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"github.com/Wei-Shaw/sub2api/ent/permission"
	"github.com/Wei-Shaw/sub2api/ent/predicate"
	"github.com/Wei-Shaw/sub2api/ent/serviceprincipal"
	"github.com/Wei-Shaw/sub2api/ent/serviceprincipalworkerpermission"
)

// ServicePrincipalWorkerPermissionQuery is the builder for querying ServicePrincipalWorkerPermission entities.
type ServicePrincipalWorkerPermissionQuery struct {
	config
	ctx                  *QueryContext
	order                []serviceprincipalworkerpermission.OrderOption
	inters               []Interceptor
	predicates           []predicate.ServicePrincipalWorkerPermission
	withServicePrincipal *ServicePrincipalQuery
	withPermission       *PermissionQuery
	modifiers            []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ServicePrincipalWorkerPermissionQuery builder.
func (_q *ServicePrincipalWorkerPermissionQuery) Where(ps ...predicate.ServicePrincipalWorkerPermission) *ServicePrincipalWorkerPermissionQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *ServicePrincipalWorkerPermissionQuery) Limit(limit int) *ServicePrincipalWorkerPermissionQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *ServicePrincipalWorkerPermissionQuery) Offset(offset int) *ServicePrincipalWorkerPermissionQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *ServicePrincipalWorkerPermissionQuery) Unique(unique bool) *ServicePrincipalWorkerPermissionQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *ServicePrincipalWorkerPermissionQuery) Order(o ...serviceprincipalworkerpermission.OrderOption) *ServicePrincipalWorkerPermissionQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryServicePrincipal chains the current query on the "service_principal" edge.
func (_q *ServicePrincipalWorkerPermissionQuery) QueryServicePrincipal() *ServicePrincipalQuery {
	query := (&ServicePrincipalClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(serviceprincipalworkerpermission.Table, serviceprincipalworkerpermission.ServicePrincipalColumn, selector),
			sqlgraph.To(serviceprincipal.Table, serviceprincipal.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, serviceprincipalworkerpermission.ServicePrincipalTable, serviceprincipalworkerpermission.ServicePrincipalColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryPermission chains the current query on the "permission" edge.
func (_q *ServicePrincipalWorkerPermissionQuery) QueryPermission() *PermissionQuery {
	query := (&PermissionClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(serviceprincipalworkerpermission.Table, serviceprincipalworkerpermission.PermissionColumn, selector),
			sqlgraph.To(permission.Table, permission.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, serviceprincipalworkerpermission.PermissionTable, serviceprincipalworkerpermission.PermissionColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first ServicePrincipalWorkerPermission entity from the query.
// Returns a *NotFoundError when no ServicePrincipalWorkerPermission was found.
func (_q *ServicePrincipalWorkerPermissionQuery) First(ctx context.Context) (*ServicePrincipalWorkerPermission, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{serviceprincipalworkerpermission.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *ServicePrincipalWorkerPermissionQuery) FirstX(ctx context.Context) *ServicePrincipalWorkerPermission {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// Only returns a single ServicePrincipalWorkerPermission entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ServicePrincipalWorkerPermission entity is found.
// Returns a *NotFoundError when no ServicePrincipalWorkerPermission entities are found.
func (_q *ServicePrincipalWorkerPermissionQuery) Only(ctx context.Context) (*ServicePrincipalWorkerPermission, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{serviceprincipalworkerpermission.Label}
	default:
		return nil, &NotSingularError{serviceprincipalworkerpermission.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *ServicePrincipalWorkerPermissionQuery) OnlyX(ctx context.Context) *ServicePrincipalWorkerPermission {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// All executes the query and returns a list of ServicePrincipalWorkerPermissions.
func (_q *ServicePrincipalWorkerPermissionQuery) All(ctx context.Context) ([]*ServicePrincipalWorkerPermission, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ServicePrincipalWorkerPermission, *ServicePrincipalWorkerPermissionQuery]()
	return withInterceptors[[]*ServicePrincipalWorkerPermission](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *ServicePrincipalWorkerPermissionQuery) AllX(ctx context.Context) []*ServicePrincipalWorkerPermission {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// Count returns the count of the given query.
func (_q *ServicePrincipalWorkerPermissionQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*ServicePrincipalWorkerPermissionQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *ServicePrincipalWorkerPermissionQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *ServicePrincipalWorkerPermissionQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.First(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *ServicePrincipalWorkerPermissionQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ServicePrincipalWorkerPermissionQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *ServicePrincipalWorkerPermissionQuery) Clone() *ServicePrincipalWorkerPermissionQuery {
	if _q == nil {
		return nil
	}
	return &ServicePrincipalWorkerPermissionQuery{
		config:               _q.config,
		ctx:                  _q.ctx.Clone(),
		order:                append([]serviceprincipalworkerpermission.OrderOption{}, _q.order...),
		inters:               append([]Interceptor{}, _q.inters...),
		predicates:           append([]predicate.ServicePrincipalWorkerPermission{}, _q.predicates...),
		withServicePrincipal: _q.withServicePrincipal.Clone(),
		withPermission:       _q.withPermission.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithServicePrincipal tells the query-builder to eager-load the nodes that are connected to
// the "service_principal" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *ServicePrincipalWorkerPermissionQuery) WithServicePrincipal(opts ...func(*ServicePrincipalQuery)) *ServicePrincipalWorkerPermissionQuery {
	query := (&ServicePrincipalClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withServicePrincipal = query
	return _q
}

// WithPermission tells the query-builder to eager-load the nodes that are connected to
// the "permission" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *ServicePrincipalWorkerPermissionQuery) WithPermission(opts ...func(*PermissionQuery)) *ServicePrincipalWorkerPermissionQuery {
	query := (&PermissionClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withPermission = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		ServicePrincipalID int64 `json:"service_principal_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ServicePrincipalWorkerPermission.Query().
//		GroupBy(serviceprincipalworkerpermission.FieldServicePrincipalID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *ServicePrincipalWorkerPermissionQuery) GroupBy(field string, fields ...string) *ServicePrincipalWorkerPermissionGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ServicePrincipalWorkerPermissionGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = serviceprincipalworkerpermission.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		ServicePrincipalID int64 `json:"service_principal_id,omitempty"`
//	}
//
//	client.ServicePrincipalWorkerPermission.Query().
//		Select(serviceprincipalworkerpermission.FieldServicePrincipalID).
//		Scan(ctx, &v)
func (_q *ServicePrincipalWorkerPermissionQuery) Select(fields ...string) *ServicePrincipalWorkerPermissionSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &ServicePrincipalWorkerPermissionSelect{ServicePrincipalWorkerPermissionQuery: _q}
	sbuild.label = serviceprincipalworkerpermission.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ServicePrincipalWorkerPermissionSelect configured with the given aggregations.
func (_q *ServicePrincipalWorkerPermissionQuery) Aggregate(fns ...AggregateFunc) *ServicePrincipalWorkerPermissionSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *ServicePrincipalWorkerPermissionQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !serviceprincipalworkerpermission.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *ServicePrincipalWorkerPermissionQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ServicePrincipalWorkerPermission, error) {
	var (
		nodes       = []*ServicePrincipalWorkerPermission{}
		_spec       = _q.querySpec()
		loadedTypes = [2]bool{
			_q.withServicePrincipal != nil,
			_q.withPermission != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ServicePrincipalWorkerPermission).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ServicePrincipalWorkerPermission{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withServicePrincipal; query != nil {
		if err := _q.loadServicePrincipal(ctx, query, nodes, nil,
			func(n *ServicePrincipalWorkerPermission, e *ServicePrincipal) { n.Edges.ServicePrincipal = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withPermission; query != nil {
		if err := _q.loadPermission(ctx, query, nodes, nil,
			func(n *ServicePrincipalWorkerPermission, e *Permission) { n.Edges.Permission = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *ServicePrincipalWorkerPermissionQuery) loadServicePrincipal(ctx context.Context, query *ServicePrincipalQuery, nodes []*ServicePrincipalWorkerPermission, init func(*ServicePrincipalWorkerPermission), assign func(*ServicePrincipalWorkerPermission, *ServicePrincipal)) error {
	ids := make([]int64, 0, len(nodes))
	nodeids := make(map[int64][]*ServicePrincipalWorkerPermission)
	for i := range nodes {
		fk := nodes[i].ServicePrincipalID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(serviceprincipal.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "service_principal_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *ServicePrincipalWorkerPermissionQuery) loadPermission(ctx context.Context, query *PermissionQuery, nodes []*ServicePrincipalWorkerPermission, init func(*ServicePrincipalWorkerPermission), assign func(*ServicePrincipalWorkerPermission, *Permission)) error {
	ids := make([]int64, 0, len(nodes))
	nodeids := make(map[int64][]*ServicePrincipalWorkerPermission)
	for i := range nodes {
		fk := nodes[i].PermissionID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(permission.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "permission_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (_q *ServicePrincipalWorkerPermissionQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	_spec.Unique = false
	_spec.Node.Columns = nil
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *ServicePrincipalWorkerPermissionQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(serviceprincipalworkerpermission.Table, serviceprincipalworkerpermission.Columns, nil)
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		for i := range fields {
			_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
		}
		if _q.withServicePrincipal != nil {
			_spec.Node.AddColumnOnce(serviceprincipalworkerpermission.FieldServicePrincipalID)
		}
		if _q.withPermission != nil {
			_spec.Node.AddColumnOnce(serviceprincipalworkerpermission.FieldPermissionID)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *ServicePrincipalWorkerPermissionQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(serviceprincipalworkerpermission.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = serviceprincipalworkerpermission.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range _q.modifiers {
		m(selector)
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (_q *ServicePrincipalWorkerPermissionQuery) ForUpdate(opts ...sql.LockOption) *ServicePrincipalWorkerPermissionQuery {
	if _q.driver.Dialect() == dialect.Postgres {
		_q.Unique(false)
	}
	_q.modifiers = append(_q.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return _q
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (_q *ServicePrincipalWorkerPermissionQuery) ForShare(opts ...sql.LockOption) *ServicePrincipalWorkerPermissionQuery {
	if _q.driver.Dialect() == dialect.Postgres {
		_q.Unique(false)
	}
	_q.modifiers = append(_q.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return _q
}

// ServicePrincipalWorkerPermissionGroupBy is the group-by builder for ServicePrincipalWorkerPermission entities.
type ServicePrincipalWorkerPermissionGroupBy struct {
	selector
	build *ServicePrincipalWorkerPermissionQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *ServicePrincipalWorkerPermissionGroupBy) Aggregate(fns ...AggregateFunc) *ServicePrincipalWorkerPermissionGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *ServicePrincipalWorkerPermissionGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ServicePrincipalWorkerPermissionQuery, *ServicePrincipalWorkerPermissionGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *ServicePrincipalWorkerPermissionGroupBy) sqlScan(ctx context.Context, root *ServicePrincipalWorkerPermissionQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ServicePrincipalWorkerPermissionSelect is the builder for selecting fields of ServicePrincipalWorkerPermission entities.
type ServicePrincipalWorkerPermissionSelect struct {
	*ServicePrincipalWorkerPermissionQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *ServicePrincipalWorkerPermissionSelect) Aggregate(fns ...AggregateFunc) *ServicePrincipalWorkerPermissionSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *ServicePrincipalWorkerPermissionSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ServicePrincipalWorkerPermissionQuery, *ServicePrincipalWorkerPermissionSelect](ctx, _s.ServicePrincipalWorkerPermissionQuery, _s, _s.inters, v)
}

func (_s *ServicePrincipalWorkerPermissionSelect) sqlScan(ctx context.Context, root *ServicePrincipalWorkerPermissionQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
